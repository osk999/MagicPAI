# HTTP Workflows

The page content provided covers an introductory guide to HTTP workflows. According to the material, workflows can be designed to "receive HTTP requests, send HTTP requests and write output to the HTTP response object."

The guide demonstrates a practical workflow pattern where the system accomplishes three main tasks: handling inbound HTTP requests, invoking backend APIs through HTTP calls, and returning responses to clients.

The page identifies four key HTTP activities for implementation:

* HttpEndpoint
* SendHttpRequest
* WriteHttpResponse
* SetVariable

This appears to be documentation introducing developers to fundamental HTTP workflow capabilities without providing detailed code examples or implementation specifics beyond these activity names and the basic use-case pattern described.
