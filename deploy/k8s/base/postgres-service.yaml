apiVersion: v1
kind: Service
metadata:
  name: magicpai-postgres
  namespace: magicpai
  labels:
    app.kubernetes.io/name: magicpai-postgres
    app.kubernetes.io/part-of: magicpai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: magicpai-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
